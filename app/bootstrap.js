require('globals');

module.exports = function bootstrap(server) {
  var router = require('router');
  //router.addRoute('/email/:uid/:field?/', require('controllers/email'));
  //router.addRoute('/email/:uid', require('controllers/email'));
};

